텔레그램에도 슬랙 레거시에도 표 구문이 없다. 고정폭 블록으로 내보내는데, **열 너비를
문자 수로 맞추면 한글이 든 표는 반드시 어긋난다** — 한글·한자·이모지는 한 칸이 아니라
두 칸을 차지한다.

`환경`(4칸)과 `스테이징`(8칸)을 `chars().count()` 로 재면 2 와 4 다. 그 차이만큼
열이 밀리고, 열이 셋만 넘어가면 무엇이 무엇인지 알 수 없게 된다.

정렬 지정(`:---:`, `---:`)도 함께 확인한다. 고정폭에서 정렬은 패딩을 어느 쪽에 주느냐다.
