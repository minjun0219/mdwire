줄 넘는 강조의 **실제 모양**이다. LLM 산출물 두 표본(174건)에서 강조가 줄을 넘은 85줄 중
78줄이 이 꼴이었다 — 여는 `**` 는 보통 자리, 닫는 `**` 는 다음 줄 중간. 정규식 변환기가
`[^*\n]+` 로 짝을 찾아 줄을 못 넘기고, 닫는 마커를 그 다음 여는 마커와 짝지어 범위를
뒤집던 고장이 여기서 났다.

`emphasis-across-linebreak` 는 그 고장을 재현하려고 만든 인위적 모양이고, 이 케이스가
데이터에서 본 모양이다. 둘 다 둔다.
