`** 띄운 굵게 **` — 열 수도 닫을 수도 없는 마커 둘이다. CommonMark 도 슬랙도 글자로
두는데, 앞의 `**` 를 추측으로 열어 두고 뒤의 `**` 가 그것을 "닫아" 버려서 여는 쪽은 글자로
되돌아가고 닫는 쪽만 사라졌다 — `** 배포  는` 이 됐다. 마커가 사라지는 것은 내용 손실이다.

추측으로 연 것은 공백 뒤의 마커로 닫지 않는다. `2 ** 3` 과 같은 부류다.
