에이전트가 코드펜스를 열고 닫지 않은 채 출력을 끝낸다. 토큰 한도에 걸려 잘리거나,
마지막 블록의 닫는 펜스를 빠뜨리는 식이다.

닫히지 않은 채로 내보내면 `<pre>` 가 열린 채 끝나 채널이 거절하거나(400) 뒤의 무관한
텍스트를 통째로 삼킨다. **출력 직전에 닫는다**(`SPEC.md` 6절).

펜스 안의 `|` 는 표가 아니다. 코드펜스 안에서는 어떤 블록 문법도 보지 않는다.
