[package]
name = "mdwire-core"
description = "Streaming Markdown normalizer and channel renderer. No dependencies."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# 패키지는 mdwire-core, 라이브러리 이름은 mdwire — `SPEC.md` 5절이 쓰는 이름이다.
[lib]
name = "mdwire"
path = "src/lib.rs"

# 의존성은 여기에 늘리지 않는다. 이 crate 는 std 만 쓴다.
[dependencies]
