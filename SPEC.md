# mdwire 확정안

`DESIGN.md`가 *왜*(실측 근거)를 들고 있다면, 이 문서는 *무엇을 만들 것인가*다.
**2026-09-18 확정.** 여기서 벗어나려면 이 문서를 먼저 고친다.

## 1. 한 줄

에이전트가 만든 마크다운을, 스트리밍으로, 채팅 채널과 브라우저에 깨지지 않게 내보낸다.

## 2. 범위

**한다**

- 깨진 입력 복구 — 짝 안 맞는 강조, 줄 넘는 강조, 안 닫힌 코드펜스
- 채널별 렌더링 — 타깃이 받는 문법으로
- 안전 분할 — 길이 한도와 스트리밍 경계에서 마크업을 자르지 않는다

**안 한다**

- CommonMark 스펙 100% 준수. 렌더 타깃이 못 받는 문법은 파싱하지 않는다
- 발송. 자격도 네트워크도 다루지 않는다. 문자열을 받아 문자열을 낸다
- 에이전트 출력 제약(프롬프트). 그건 이 문제의 다른 답이고, 라이브러리가 할 일이 아니다

## 3. 배치

```
mdwire/
  crates/mdwire-core/   의존 0. v0.1은 std. no_std는 벤치 뒤에 판단한다
  crates/mdwire-cli/    stdin → stdout. 단일 정적 바이너리
  crates/mdwire-wasm/   브라우저·npm 용. wasm-bindgen은 여기에만 둔다
  corpus/               입력 → 채널별 기대 출력. 정본
  go/                   지금은 없다. 필요해질 때 만든다
```

**한 레포로 간다.** 코퍼스가 정본이라는 설계는 두 구현이 같은 커밋에서 검증될 때 가장 잘
돈다. 릴리스 주기가 실제로 어긋나면 그때 뗀다.

**의존은 코어에 없고 바인딩 층에만 둔다.** 코어를 쓰는 쪽은 깨끗하게 남는다.

## 4. 채널

| 채널 | 받는 것 | 한도 |
|---|---|---|
| Telegram HTML | `b i u s code pre a blockquote tg-spoiler` | 4096 |
| Slack `markdown_text` | 표준 마크다운 그대로 | 12,000 |
| Plain | 마크업 제거. 폴백 | — |

**두 채널로 시작하는 이유**는 하나만으로는 이 라이브러리의 주장이 코드로 증명되지 않기
때문이다. 텔레그램은 실제로 깨지는 곳이고, 슬랙은 변환이 거의 없어 사실상 "정규화 + 분할"만
남는다 — 성격이 정반대인 둘이 같은 결과를 내야 "타깃 간 일관성"이 말이 된다.
`Plain`은 폴백 경로라 처음부터 있어야 한다.

## 5. API

```rust
// 완성본
let parts: Vec<String> = mdwire::render(input, Channel::TelegramHtml);

// 스트리밍 — 지금 안전하게 낼 수 있는 만큼만 돌려준다
let mut s = Streamer::new(Channel::SlackMarkdown);
out.push_str(s.push(chunk));    // 내부 버퍼를 빌려준다. 다음 호출까지 유효하다
out.push_str(s.finish());       // 남은 것을 내보내고 열린 마크업을 닫는다

// 할당이 0 이어야 하는 경로는 호출자 버퍼에 직접 쓴다
s.push_into(chunk, &mut out);
```

**스트리밍 반환 모양은 벤치가 정했다.** 변환 함수를 고정하고 서명만 바꿔 재면
(`crates/mdwire-bench`, release, 6160B 입력을 64B 조각 96개로) 이렇게 나온다.

| 후보 | 할당/조각 | 상대 처리량 |
|---|---|---|
| `push() -> String` | 4.58 | 1.0× |
| `push() -> &str` | 0.05 | 3.7× |
| `push_into(&mut String)` | **0.00** | **3.9×** |
| `push_with(\|&str\|)` | 0.05 | 3.6× |

**절대 MiB/s는 적지 않는다.** 같은 기계 같은 커밋에서도 회차마다 1.4배까지 흔들린다
(터보·발열). 할당 횟수는 결정적이고 상대비는 회차가 바뀌어도 그대로라, 고르는 기준은
그 둘이다.

조각마다 `String`을 만드는 서명은 **변환을 하기도 전에** 조각당 4.6회를 쓰고 처리량이
1/4로 떨어진다. 콜백은 싱크와 같은 값을 내면서 호출부만 뒤집어 놓으므로 버린다.

→ **`push_into`가 정본이고, `push`는 내부 버퍼를 빌려주는 편의 서명이다.**
둘은 같은 코드를 부른다.

CLI는 `mdwire --channel telegram-html [--stream]`.
분할 결과는 **NUL 로 구분**한다 — 셸에서 다루기 가장 쉽고, 마크다운 본문에 안 나오는
바이트다.

## 6. 정규화 강도

어디까지 고칠지가 곧 이 라이브러리의 성격이다. 세 단계로 나눠 둔다.

| 단계 | 하는 일 | v0.1 |
|---|---|---|
| **복구** | 안 닫힌 강조·펜스를 닫는다. 줄 넘는 강조를 하나로 본다 | **한다** |
| **정돈** | 리스트 마커 통일, 과한 빈 줄 정리, 헤딩 레벨 정규화 | **한다** |
| **각색** | 체크박스를 ✅/⬜ 로 바꾸는 류 | **안 한다** |

복구는 실측된 고장이라 이견이 없다. 정돈까지는 "같은 글이 늘 같은 모양으로 나간다"는
약속의 일부다.

**짝 잃은 마커의 규칙**(실측 2026-09-23, LLM 산출물 430건):

- 열 수 없는 자리의 마커는 **추측**으로 열어 두되, 추측은 확정되지 않는다 — 뒤의 마커가
  닫아 주지 않고, 블록 끝에서 글자로 되돌린다. 닫아 주면 여는 쪽은 사라지고 닫는 쪽만
  없어져 `underfront.*`·`/* 주석 */` 의 별표가 없어졌다(내용 손실)
- 홑마커(`*` `_`)는 글자로 남긴다 — 각주·글롭·곱셈. `**` 는 앞이 공백이었을 때만
  남긴다(`2 ** 3`). 앞이 글자인 `**` 가 홀로 남을 이유는 없다
- 여는 마커가 또 오면 **먼저 열린 쪽이 진다** — `채널**이다.⏎**신분 공개**이` 에서 첫
  `**` 는 버리고 둘째 줄이 굵게다. CommonMark 도 슬랙도 그렇게 읽는다. "닫기"로 읽으면
  범위가 뒤집힌다(`DESIGN.md` 1절)
- 마커 앞이 글자·ASCII 숫자가 아니면(공백·구두점·`①`·이모지) 열 수 있다 — LLM 은 항목
  머리에 기호를 붙인다

**각색은 하지 않는다.** 원문의 뜻을 바꾸는 일이고, 옵션으로 열면 채널 × 정책 조합이
폭발한다. 표는 예외인데, 그건 각색이 아니라 **타깃에 그 구문이 없어서 어쩔 수 없는 것**이다
(7절).

## 7. 표

텔레그램도 슬랙 레거시도 표 구문이 없다. **고정폭 블록으로 간다** — `<pre>` 안에서 열을 맞춘다. 좁은 화면에서 가로로 밀리지만
표라는 것이 남고, 무엇보다 **이 라이브러리 안에서 끝난다.**

**표를 직접 그리는 채널은 그대로 낸다.** 슬랙 `markdown_text` 는 표준 마크다운 표를 네이티브로
그린다(Slack markdown block 문서). 거기서 고정폭으로 내리면 화면에 표가 아니라 코드가 보인다 —
GFM 그대로, 정렬 표시와 셀 안의 마크업까지 살려서 낸다.

기각한 둘:

- **줄글로 펴기** — 정렬을 잃는다. 열이 셋만 넘어가도 무엇이 무엇인지 알 수 없다
- **이미지로 렌더** — 폰트·레이아웃 의존이 생기고 발송까지 관여하게 된다. 범위를 넘는다

열 너비는 **문자 폭이 아니라 표시 폭**으로 잰다. 한글·한자·이모지는 두 칸을 차지한다 —
문자 수로 맞추면 한글 표는 반드시 어긋난다.

## 8. 블록 매핑

타깃이 못 받는 블록을 무엇으로 바꾸는지가 채널 구현의 절반이다. 표(7절)와 같은 이유로
이것들은 **각색이 아니라 타깃에 그 구문이 없어서 어쩔 수 없는 것**이다.

| 입력 | Telegram HTML | Slack `markdown_text` | Plain |
|---|---|---|---|
| 헤딩 | `<b>…</b>` (구문 없음) | `#` 유지, 레벨 정규화 | 글자만 |
| 불릿 | `• ` | `- `로 통일 | `• ` |
| 번호 | `1. ` | `1. ` | `1. ` |
| 인용 | `<blockquote>`로 감싼다 | `> ` | `> ` |
| 코드펜스 | `<pre>` / `<pre><code class="language-…">` | ` ``` ` 유지 | 펜스 제거, 내용만 |
| 표 | `<pre>` 안 고정폭 | GFM 그대로 | 고정폭 |
| 구분선 | `──────────` | `---` | `──────────` |
| 링크 | `<a href>` | `[텍스트](url)` | `텍스트 (url)` |
| `<url>` · `<url\|텍스트>` | `<a href>` | `<url>` · `[텍스트](url)` | `url` · `텍스트 (url)` |
| 인라인 HTML (`<sub>` `<br>` 주석) | 태그만 벗긴다 · `<br>` 은 줄바꿈 | 같음 | 같음 |

**인라인 HTML 은 아는 태그만 벗긴다.** LLM 은 마크다운에 없는 표현을 `<sub>`·`<br>` 로
메운다(실측 표본에서 74건). 텔레그램은 모르는 태그에 400 을 주고 슬랙 `markdown_text` 도
그리지 않으니, escape 하면 화면에 `<sub>` 가 글자로 보인다. 태그는 벗기고 내용은 둔다 —
각색이 아니라 표처럼 타깃에 그 구문이 없어서다. `Vec<T>` · `1 < 2` 처럼 모르는 꺾쇠는
글이고, `<a>` 는 벗기면 주소가 사라져 손대지 않는다.

**`<url>` 은 링크다.** 슬랙에서 긁어 온 글에 레거시 `<url|텍스트>` 가 그대로 남고(한 표본에서
124건), 텍스트 없는 `<url>` 은 CommonMark 오토링크다. 텍스트는 인라인으로 다시 읽지 않는다.

**헤딩 레벨 정규화**는 스트리밍이 가능한 범위까지만 한다 — 앞 헤딩보다 두 단계 이상
깊어지면 한 단계로 당긴다. "문서에서 가장 얕은 헤딩을 1로" 같은 규칙은 문서 전체를 봐야
하므로 하지 않는다.

**링크는 한 줄 안에서만 인식한다.** `[텍스트](url)`이 줄을 넘으면 글자로 둔다. 실측에서
링크가 wrap 된 사례가 없었고, 여기에 상태를 하나 더 두는 값이 없다.

**표는 `|`로 시작하는 줄에서만 시작한다.** 앞 블록이 무엇이든 상관없다. 구분선이
따라오는지 보려면 그 줄 하나를 더 붙들어야 하는데, 대상이 `|`로 시작하는 줄뿐이면
산문은 한 번도 안 걸린다. 반대로 "줄 어디에든 `|`가 있으면 표일 수 있다"로 넓히면
모든 줄을 끝까지 붙들어야 하고, 그러면 줄바꿈 없이 한 문단을 쏟는 에이전트 앞에서
이 라이브러리는 배치형이 된다.

**인용 안에서는 블록이 다시 열리지 않는다.** `> ` 뒤는 인라인으로만 읽는다 — 인용
안의 표도, 코드펜스도 블록으로 안 잡힌다. `> ```yaml` 은 펜스가 아니라 코드 스팬으로
읽혀서 info 문자열이 내용으로 새고 `<pre>` 대신 `<code>` 로 나간다. 고치려면 블록
층이 접두사를 벗긴 나머지를 다시 분류해야 하는데, 그건 스트리밍 상태를 한 겹 더
쌓는 일이라 v0.1 에서는 하지 않는다. 실측 2289건에서 이 모양은 한 문서에서 나왔다.

**앞에 `|`가 없는 표는 문단으로 나간다.** 실제 문서 2281개에서 표 951개 중 950개가
`|`로 시작했다(에이전트 산출물은 21개 전부). 남은 하나를 위해 스트리밍을 포기할 값이 없다.

**한도를 넘겨 나눌 때 열린 인라인 마크업은 끊는 자리에서 닫고 다음 조각에서 다시 연다.**
텔레그램의 태그만이 아니라 슬랙의 `**`·`*`·`~~`·백틱도 같다 — 12,000자 분할이
`` `main → main` `` 의 공백에 떨어지면 두 메시지 다 코드가 깨진다. 짝이 맞는 스팬만 그렇게
다룬다. 글자로 남은 마커(`2 ** 3`, 한 줄에 쏟아낸 가짜 펜스의 ```)까지 스팬으로 보면 조각마다
없던 마커가 찍힌다 — 실측이다.

### 8.1 스트리밍이 붙드는 것

"안전하게 낼 수 있는 만큼"이 어디까지인지가 스트리밍의 실제 계약이다. 붙드는 것은 셋뿐이다.

| 붙드는 것 | 언제까지 | 왜 |
|---|---|---|
| 판정이 덜 된 접두사 | 몇 글자 | `#` 뒤에 공백이 올지 글자가 올지 봐야 한다 |
| 줄 끝에 걸친 마커·공백 | 다음 글자까지 | `**`는 다음 글자를 봐야 열기/닫기가 갈린다 |
| 열린 강조의 안쪽 | 짝이 맞을 때까지 | 반쪽으로 내보내면 뒤의 무관한 텍스트를 삼킨다 |
| 코드펜스 줄 · 표 | 줄 끝 · 표 끝 | info 문자열과 열 너비는 끝까지 봐야 안다 |

**문단은 붙들지 않는다.** 줄이 끝나야 내보내는 구현은 스트리밍이라고 부를 수 없다.

**블록의 여는 마크업은 붙들지 않는다.** `<blockquote>`·`<pre>`·헤딩의 `<b>`는 그 블록이
끝나기 전에 나간다 — 코드블록 하나가 끝날 때까지 출력을 멈추면 스트리밍이 아니다.
대신 누적본을 중간에 채널로 보내는 쪽(토큰이 오는 대로 메시지를 편집하는 경우)을 위해
`Streamer::close_open`이 **지금 열린 것만 닫아 준다.** 상태는 건드리지 않으므로 붙인
뒤에도 스트리밍은 이어진다.

| | 누적본 | `close_open` 붙인 것 |
|---|---|---|
| 인라인 강조 | 균형 맞음(붙들려 있다) | 같음 |
| 블록 마크업 | **열려 있을 수 있음** | 균형 맞음 |
| 그대로 채널에 보내도 되나 | 아니오 | 예 |

## 9. 성능

성능이 기능보다 앞선다. 스트리밍 경로는 조각마다 불린다.

- 입력 1회 순회. 재파싱 금지
- **벤치를 먼저 세우고 API를 확정한다.** 반대 순서로 하면 서명에 갇힌다 — 5절이 그 결과다

**목표치는 첫 수치를 보고 정했다.** release, 64B 조각 기준. 한 회차에서 함께 뽑은 값이다.

| 경로 | 할당/조각 | 상대 처리량 |
|---|---|---|
| 산문 스트리밍 (버퍼 재사용) | **0.00** | 1.9× |
| 산문 스트리밍 (새 `Streamer`) | 0.09 | 1.9× |
| 표·코드펜스 섞인 문서 | 6.1 | 1.0× |

**게이트를 할당에 거는 이유가 여기 있다.** 처리량은 같은 커밋에서도 회차마다 1.4배까지
흔들려서 임계값을 걸면 거짓 경보만 난다. 할당 횟수는 결정적이다.

**게이트는 첫 줄 하나다 — 버퍼가 자란 뒤 산문을 흘릴 때 할당이 0.**
`cargo test`가 지킨다(`crates/mdwire-bench`의 `prose_streaming_is_allocation_free_once_warm`).
0이 아니게 되는 변경은 조각마다 불리는 경로에 할당을 들인 것이다.

표가 셀 수만큼 할당하는 것은 회귀가 아니다. **열 너비를 재려면 어차피 표 전체를 봐야
한다** — 여기는 스트리밍이 아니고, 그래서 게이트도 걸지 않는다.

순위표는 만들지 않는다. 다른 구현과 나란히 재면 대부분 **언어 차이**를 재게 된다.
이 수치의 쓸모는 둘뿐이다 — 절대값이 목표를 만족하는가, 그리고 회귀했는가.

## 10. 코퍼스

`corpus/cases/<이름>/`에 `input.md`와 채널별 기대 출력, 그리고 `why.md`.
**실제로 겪은 고장만 넣는다.** 가상 엣지 케이스는 스펙 준수 테스트지 이 라이브러리의 목표가
아니다.

## 11. v0.1

- 채널 셋: Telegram HTML · Slack `markdown_text` · Plain
- 정규화: 복구 + 정돈
- 표: 고정폭 블록 (표시 폭 기준) · 직접 그리는 채널은 GFM 그대로
- 분할: 길이 한도 + 스트리밍 경계
- CLI + WASM 바인딩
- 코퍼스: 위 전부에 케이스

## 12. 이후로 미룬 것

정해서 미룬 것이지 안 정한 것이 아니다.

- Telegram MarkdownV2 · Slack 레거시 `mrkdwn` — **미룬 것이 아니라 뺐다**(2026-09-22).
  목표는 텔레그램 HTML 과 슬랙 `markdown_text` 둘이고, 레거시 표기 때문에 남겨 두던 CJK
  패딩(`CjkPolicy`)도 같이 걷어냈다. 실측에서 두 목표 채널 어디에도 U+200B 가 필요 없었다
  (`DESIGN.md` 3절)
- `no_std` 전환 — 지금 wasm 번들은 105 KB(release, `wasm-opt` 전)다. 이 중 얼마가
  `std` 이고 얼마가 `wasm-bindgen` 인지 가른 뒤에 판단한다. 숫자를 가르기 전에
  옮기면 얻는 것 없이 코어만 불편해진다
- Go 구현 — 코퍼스가 정본이니 생길 때 포팅한다. FFI는 쓰지 않는다
- **YAML frontmatter** — 떼지 않는다. 걸리는 것은 붙드는 **크기**가 아니라 **자리**다.
  8.1 이 붙드는 것들은 전부 저자가 이미 연 구문의 안쪽이고 그 앞은 나간 뒤다 — 표를 표
  끝까지 붙들어도 표 앞 문단은 이미 갔다. frontmatter 는 문서 맨 앞이라 붙드는 순간
  **첫 글자부터 아무것도 못 나간다.** 게다가 `---` 가 여는 줄인지 구분선인지는 닫는
  `---` 를 찾아야 갈리는데, 구분선이면 그 줄은 영영 안 온다. 문서 끝까지 붙들고 나서
  "그냥 구분선이었다"로 끝날 수 있다는 뜻이다. 강조는 내용을 먼저 내보내고 마커만
  나중에 되돌리는 수가 있지만, 여기서는 **내용 자체가 판정 대상**이라 그 수를 못 쓴다.
  떼야 하는 쪽은 호출자가 이미 알고 있고 거기서는 세 줄이다
- **코드 스팬 내용의 이중 보관** — 짝 없는 백틱 런을 글자로 되돌리려면 날것 내용을
  들고 있어야 해서, 지금은 출력(escape 된 것)과 버퍼(날것)에 한 번씩 쓴다. 없애려면
  스팬의 결과가 정해질 때까지 출력을 미루고, 닫을 때와 되돌릴 때 escape 여부가 갈리는
  두 종류를 구분해 들고 있어야 한다. **먼저 재고 미뤘다** — 코드 스팬만 가득한 11 MB
  입력(줄마다 스팬 2개)에서 60 → 59 MiB/s, 산문은 차이가 없고 할당 게이트도 그대로다
