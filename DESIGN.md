# 설계 노트

구현 전에 정해 둔 것과, 그 근거가 된 실측.

## 파싱 범위는 출력이 정한다

CommonMark 전체를 지원할 이유가 없다. **렌더 타깃이 못 받는 문법은 파싱할 이유도 없기
때문이다.** 채널이 받는 것이 우리가 다룰 것의 상한이다.

| 채널 | 받는 것 | 한도 |
|---|---|---|
| Telegram HTML | `b i u s code pre a blockquote tg-spoiler` 9개 태그. 표·헤딩 없음 | 4096 |
| Slack `markdown_text` | 표준 마크다운을 슬랙이 직접 변환 | 12,000 |

실제 에이전트 출력 60개를 세어 보면 들어오는 문법도 좁다 — 표 0건, 코드펜스 0건,
중첩 리스트 0건, 취소선 0건. 실제로 들어온 것은 헤딩 · 굵게 · 인라인 코드 · 불릿 ·
번호 리스트 · 인용이었다. 다만 **에이전트에 따라 표와 체크박스는 흔히 나오므로** 범위에서
빼지는 않는다.

**이 0건들을 "그 문법은 드물다"로 읽으면 안 된다.** 표본은 한 종류의 문서(정기 브리프)고,
같은 에이전트라도 용도가 바뀌면 나오는 문법이 바뀐다. 실제로 취소선은 LLM 응답에서 흔한
편인데 이 표본에는 0건이었다. 이 숫자들이 말해 주는 것은 **이 표본에서 무엇이 깨졌는가**
뿐이고, 어떤 문법을 지원하지 않을 근거로는 쓸 수 없다.

## 고쳐야 할 것 — 실측으로 확인된 고장

### 1. 줄을 넘는 강조 (가장 흔하고, 가장 조용하다)

80열로 wrap 된 산문에서 `**강조**`가 줄을 넘는 것은 예외가 아니라 일상이다.
표본 60개 중 44개에 있었다.

정규식 기반 변환기(`/\*\*([^*\n]+)\*\*/g`)는 이걸 못 잡고 `**`를 원문 그대로 남긴다.
여기까지면 "안 굵어지고 만다"로 끝나는데, **짝을 잃은 `**`가 뒤의 `**`와 잘못 짝지어져
강조 범위가 뒤집힌다.** 표본의 47%(28/60)에서 실제로 일어났다.

```
원문    …공개 채널**이다. 글 내용이 아니라 **신분 공개…
배달됨  …공개 채널<b>이다. 글 내용이 아니라 </b>신분 공개…
```

**짝이 안 맞아도 유효한 HTML 이라 채널이 200을 준다.** 폴백도 안 걸리고 로그에도 안 남는다.
관측되지 않는 고장이라 몇 달을 갈 수 있다.

→ 정규식이 아니라 인라인 파서로 짝을 맞춰야 한다. 그리고 닫히지 않은 강조는
**출력 직전에 닫는다** — 열린 채 내보내면 뒤의 무관한 텍스트를 삼킨다.

### 2. 마크업을 만든 뒤에 자르기

길이 제한을 변환 **후** 문자 수로 자르면 `<code>`가 열린 채 잘린다. 채널은 400을 주고
폴백으로 평문이 나간다 — 배달은 되지만 포맷이 통째로 사라진다.

스트리밍에서도 같은 고장이 다른 얼굴로 나온다. 64자 단위로 흘려보내면 `**굵게**`가
`**굵` / `게**`로 갈린다.

→ **자르는 지점은 렌더 결과가 아니라 구조에서 고른다.** 열린 마크업이 없는 지점만 경계가
될 수 있고, 없으면 그 앞에서 닫고 다음 조각에서 다시 연다.

### 3. CJK 인접 강조 — 재 보니 고장이 아니었다

한글은 조사가 붙으니 강조 뒤에 글자가 오는 것이 기본이다(`**금요일**에`). 이 자리를
채널 파서가 강조로 못 잡는다는 관측이 있었고, 기존 변환기 한쪽은 U+200B 를 끼우고 한쪽은
안 끼웠다(실제 문서 234개에서 6,102 대 0). 처음에는 그 "제각각"을 채널별 정책으로 노출하는
것이 답이라고 봤다.

**실측하니 목표 채널 어디에도 해당하지 않았다**(2026-09-22). 슬랙 `markdown_text` 로
`chat.postMessage` 를 보내고 저장된 rich_text 를 읽었다:

| 보낸 것 | 저장된 것 |
|---|---|
| `**마통**이 0이고 **잔액**은` | 셋 다 `bold` |
| `*진료*가` / `~~취소~~가` | `italic` / `strike` |
| `_진료_가` | 글자 그대로 — `_` 가 본문에 남는다 |
| `snake_case_name` | 글자 그대로 |

CommonMark 그대로다. `*` 계열은 단어 안에서도 닫히고 `_` 만 못 닫는데, 조사가 붙은 자리가
정확히 "단어 안"이다. 그 관측은 레거시 `mrkdwn` 의 것이었고, 텔레그램 HTML 은 태그로
나가니 애초에 마커가 없다.

→ **U+200B 는 끼우지 않고 정책도 없앴다.** 기울임은 `_` 대신 `*` 로 내보낸다. 표는
슬랙이 직접 그리니 GFM 그대로 통과시킨다. 한 가지 덤 — 한글은 모바일 폰트에 이탤릭이
없어서 `*진료*가` 는 마커만 사라지고 기울어 보이지 않는다.

## 범위는 LLM 산출물이 정한다

목표는 하나다 — **LLM 이 어떻게 쏟아내도 슬랙과 텔레그램에 제대로 보인다.** 그래서 무엇을
다룰지는 실제 LLM 산출물을 세어서 정한다. 2026-09-22, 세 종류 430건(정기 브리프 152 ·
대화 요약 256 · 장문 메모 22)을 센 결과다.

| 나온 것 | 건수 | 슬랙 `markdown_text` | 텔레그램 HTML | 처리 |
|---|---:|---|---|---|
| 줄 넘는 `**` (wrap) | 85줄 | 그림 | 태그 | 78/85 가 `**a⏎b**` 꼴 — 짝 맞추기 |
| 4,096자 초과 | 29건 | — | 분할 필수 | 마크업 경계에서 분할 |
| 중첩 불릿 | 7,700줄 | 그림 | 구문 없음 | 들여쓰기 유지 |
| 헤딩 | 2,850 | 그림 | 구문 없음 | `<b>` |
| 표 | 21건 | 그림 | 구문 없음 | GFM 그대로 / `<pre>` |
| `~40km` 물결 | 243 | 취소선 아님 | — | `~~` 만 취소선 |
| `_기울임_가` | 16 | **글자로 남음** | — | `*` 로 낸다 |
| `<https://url\|텍스트>` | 124 | 링크 | `&lt;` 로 보임 | 링크로 |
| `<sub>` `<br>` 같은 HTML | 74 | 글자로 보임 | 태그가 글자로 보임 | 태그만 벗김 |
| 짝 없는 백틱 | 16블록 | 블록 끝까지 코드 | 같음 | 글자로 되돌림 |
| 표 칸 안 `\|` (코드 스팬) | 11 | 칸 갈림 | 같음 | 마지막 칸에 합침 |
| `** 띄운 굵게 **` | 3블록 | 글자 | — | 글자로 둔다 |
| 탭 뒤 마커 · 백틱 안 백틱 | **0** | | | 사람 문서에서만 나온다 |

**깨진 마크다운을 슬랙이 어떻게 그리는지도 쟀다**(같은 날, `markdown_text` 원문 대 mdwire
출력). 짝 없는 `**` 는 슬랙이 글자로 남기고 mdwire 는 블록 끝까지 굵게 한다. 안 닫힌
코드펜스는 둘 다 문서 끝까지 코드다. `_기울임_가` 는 슬랙이 글자로 남기고 mdwire 는 기울인다.
줄 넘는 `**` 는 데이터의 78/85 꼴(`**a⏎b**`)을 슬랙도 그대로 그린다 — 정규식 변환기만
못 하던 것이다.

**하네스의 척도도 이쪽이다.** 사람이 쓴 문서 2,316건으로 재던 것을 이 430건으로 옮긴다.
탭 뒤 마커, 백틱 안 백틱 같은 최근 발견은 전부 사람 문서 특유의 것이었다 — 코드는 두되
그쪽을 더 파지 않는다.

## 코퍼스가 정본이다

`corpus/`가 "입력 → 채널별 기대 출력"을 들고 있다. 구현이 하나든 넷이든, 통과 기준은
이것 하나다. 다른 언어로 포팅할 때 FFI 없이 일관성을 얻는 방법이기도 하다.

케이스를 더할 때는 **실제로 겪은 고장만** 넣는다. 가상의 엣지 케이스는 스펙 준수 테스트지
이 라이브러리의 목표가 아니다.
