# 설계 노트

구현 전에 정해 둔 것과, 그 근거가 된 실측.

## 파싱 범위는 출력이 정한다

CommonMark 전체를 지원할 이유가 없다. **렌더 타깃이 못 받는 문법은 파싱할 이유도 없기
때문이다.** 채널이 받는 것이 우리가 다룰 것의 상한이다.

| 채널 | 받는 것 | 한도 |
|---|---|---|
| Telegram HTML | `b i u s code pre a blockquote tg-spoiler` 9개 태그. 표·헤딩 없음 | 4096 |
| Telegram MarkdownV2 | 이스케이프 대상 18자. 표·헤딩 없음 | 4096 |
| Slack `markdown_text` | 표준 마크다운을 슬랙이 직접 변환 | 12,000 |
| Slack `mrkdwn` (레거시) | `*굵게*` `_기울임_` `<url\|text>`. 헤딩·표 없음 | 12,000 |

실제 에이전트 출력 60개를 세어 보면 들어오는 문법도 좁다 — 표 0건, 코드펜스 0건,
중첩 리스트 0건, 취소선 0건. 실제로 들어온 것은 헤딩 · 굵게 · 인라인 코드 · 불릿 ·
번호 리스트 · 인용이었다. 다만 **에이전트에 따라 표와 체크박스는 흔히 나오므로** 범위에서
빼지는 않는다.

**이 0건들을 "그 문법은 드물다"로 읽으면 안 된다.** 표본은 한 종류의 문서(정기 브리프)고,
같은 에이전트라도 용도가 바뀌면 나오는 문법이 바뀐다. 실제로 취소선은 LLM 응답에서 흔한
편인데 이 표본에는 0건이었다. 이 숫자들이 말해 주는 것은 **이 표본에서 무엇이 깨졌는가**
뿐이고, 어떤 문법을 지원하지 않을 근거로는 쓸 수 없다.

## 고쳐야 할 것 — 실측으로 확인된 고장

### 1. 줄을 넘는 강조 (가장 흔하고, 가장 조용하다)

80열로 wrap 된 산문에서 `**강조**` 가 줄을 넘는 것은 예외가 아니라 일상이다.
표본 60개 중 44개에 있었다.

정규식 기반 변환기(`/\*\*([^*\n]+)\*\*/g`)는 이걸 못 잡고 `**` 를 원문 그대로 남긴다.
여기까지면 "안 굵어지고 만다"로 끝나는데, **짝을 잃은 `**` 가 뒤의 `**` 와 잘못 짝지어져
강조 범위가 뒤집힌다.** 표본의 47%(28/60)에서 실제로 일어났다.

```
원문    …공개 채널**이다. 글 내용이 아니라 **신분 공개…
배달됨  …공개 채널<b>이다. 글 내용이 아니라 </b>신분 공개…
```

**짝이 안 맞아도 유효한 HTML 이라 채널이 200 을 준다.** 폴백도 안 걸리고 로그에도 안 남는다.
관측되지 않는 고장이라 몇 달을 갈 수 있다.

→ 정규식이 아니라 인라인 파서로 짝을 맞춰야 한다. 그리고 닫히지 않은 강조는
**출력 직전에 닫는다** — 열린 채 내보내면 뒤의 무관한 텍스트를 삼킨다.

### 2. 마크업을 만든 뒤에 자르기

길이 제한을 변환 **후** 문자 수로 자르면 `<code>` 가 열린 채 잘린다. 채널은 400 을 주고
폴백으로 평문이 나간다 — 배달은 되지만 포맷이 통째로 사라진다.

스트리밍에서도 같은 고장이 다른 얼굴로 나온다. 64자 단위로 흘려보내면 `**굵게**` 가
`**굵` / `게**` 로 갈린다.

→ **자르는 지점은 렌더 결과가 아니라 구조에서 고른다.** 열린 마크업이 없는 지점만 경계가
될 수 있고, 없으면 그 앞에서 닫고 다음 조각에서 다시 연다.

### 3. CJK 인접 강조

한글 옆에 붙은 `**` 를 채널 파서가 강조로 못 잡는 경우가 있다. U+200B 를 끼우면 살아난다.
문제는 **기존 변환기들이 이 정책을 제각각 갖고 있고 바꿀 수 없다**는 것이다 — 한쪽은 끼우고
한쪽은 안 끼운다. 같은 글을 두 채널에 보내면 다르게 보인다.

→ 정책을 채널별 기본값 + 사용자 오버라이드로 노출한다(`CjkPolicy`).

## 코퍼스가 정본이다

`corpus/` 가 "입력 → 채널별 기대 출력"을 들고 있다. 구현이 하나든 넷이든, 통과 기준은
이것 하나다. 다른 언어로 포팅할 때 FFI 없이 일관성을 얻는 방법이기도 하다.

케이스를 더할 때는 **실제로 겪은 고장만** 넣는다. 가상의 엣지 케이스는 스펙 준수 테스트지
이 라이브러리의 목표가 아니다.
